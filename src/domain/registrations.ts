import User from "./user";

export default class Registrations {
  user: User;
  name: string;
  birthdar: Date;
  cep: string;
  address: string;
  city: string;
  state: string;
  weight: number;
  height: number;
  createdAt: Date;
  updatedAt: Date;
}
